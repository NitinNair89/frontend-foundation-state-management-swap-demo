# Contributing

## Guidelines

- Do not import clients or stores directly into UI components
- Keep business logic inside services
- Add new implementations behind existing interfaces
- Maintain one-way dependencies

Pull requests that break architectural boundaries will be rejected.
